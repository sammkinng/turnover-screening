import React from "react";
import { Metadata } from "next";
import Page from ".";

export const metadata: Metadata = {
  title: "Login to ECOMMERCE - Access Your Business Marketplace Account",
  description:
    "Welcome back to ECOMMERCE, the next-gen business marketplace. Log in to access your account, explore categories, and take advantage of exclusive sale and clearance events.",
};

export default function LoginPage() {
  return <Page />;
}
